# ── Web framework ───────────────────────────────────────────────────────────
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.9.2

# ── MedDeID de-identification engine ────────────────────────────────────────
# Pure CV + rule based — no torch/transformers, so the image stays small.
opencv-python-headless==4.10.0.84   # cv2: image processing, connected components
numpy>=1.26,<3                       # array manipulation (pixel + DICOM)
Pillow==11.0.0                       # image IO + EXIF
pytesseract==0.3.13                  # OCR backend (system tesseract required)
docling>=2.0,<3                      # OCR waterfall engine 1 (structural PDF OCR)
paddleocr>=2.7                       # OCR waterfall engine 2 (PaddlePaddle OCR)
pydicom==3.0.1                       # DICOM read/write + tag scrub
pylibjpeg>=2.0                       # DICOM pixel data decompression framework
pylibjpeg-openjpeg>=2.0              # JPEG2000 transfer syntax (mammography etc.)
pylibjpeg-libjpeg>=2.0               # JPEG transfer syntax (older DICOMs)
nibabel==5.3.2                       # NIfTI (.nii / .nii.gz) volumes
pymupdf==1.24.13                     # fitz: PDF render + rewrite

# ── Storage abstraction (GCS optional, used when STORAGE_BACKEND=gcs) ────────
google-cloud-storage==2.18.2

# ── Misc ────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Auth (JWT bearer tokens + optional Keycloak JWKS) ───────────────────────
python-jose[cryptography]==3.3.0
email-validator==2.1.1
httpx==0.27.0

# ── Task queue (Celery + Redis) ──────────────────────────────────────────────
celery[redis]>=5.3.0,<6
redis>=5.0.0,<6

# ── Observability (Phase 3) ──────────────────────────────────────────────────
prometheus_client>=0.20.0
